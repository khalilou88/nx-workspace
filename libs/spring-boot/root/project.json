{
  "name": "root-aggregator-project",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "executor": "@jnxplus/nx-maven:run-task",
      "outputs": [
        "{options.outputDirLocalRepo}"
      ],
      "options": {
        "task": "install -N"
      }
    }
  }
}
